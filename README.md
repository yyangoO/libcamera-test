# libcamera-tests
some tests of [libcamera](https://github.com/kbingham/libcamera)

reference
 - [simple-cam](https://github.com/kbingham/simple-cam)

build & run

```
mkdir build & cd build
cmake ..
make
sudo ./libcamera_tests
```